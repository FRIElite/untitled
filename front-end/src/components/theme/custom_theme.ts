import { theme } from '@chakra-ui/core';

type Colors = {

};

const light: Colors = {

};

const dark: Colors = {

};

export default {
    ...theme,
    colors: {
        ...theme.colors,
        light,
        dark
    },
};
